[tools]
ansible = { version = "13.6.0", uvx = "true" }

[hooks]
postinstall = "[ -f requirements.yml ] && ansible-galaxy collection install -r requirements.yml || true"

[tasks.install]
description = "Install Ansible Galaxy collections"
run = "[ -f requirements.yml ] && ansible-galaxy collection install -r requirements.yml || true"
