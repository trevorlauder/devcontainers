{
  "id": "base",
  "version": "1.2.6",
  "name": "Base Development Environment",
  "description": "Base devcontainer image layer",
  "options": {
    "timezone": {
      "type": "string",
      "default": "America/Edmonton",
      "description": "Container timezone"
    },
    "chezmoiVersion": {
      "type": "string",
      "default": "2.70.0",
      "description": "chezmoi version to install"
    },
    "username": {
      "type": "string",
      "default": "vscode",
      "description": "Username to create and use as the container user."
    },
    "homeDirs": {
      "type": "string",
      "default": "",
      "description": "Comma-separated list of directories relative to $HOME to create as the container user (e.g. '.foo,.bar/baz')."
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "anthropic.claude-code",
        "eamodio.gitlens",
        "github.vscode-pull-request-github",
        "ms-azuretools.vscode-containers",
        "ms-python.python"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "zsh",
        "terminal.integrated.profiles.linux": {
          "zsh": {
            "path": "zsh",
            "args": ["-l"]
          }
        }
      }
    }
  },
  "containerEnv": {
    "LANG": "C.UTF-8",
    "LC_ALL": "C.UTF-8",
    "DEVCONTAINER": "true",
    "SHELL": "/usr/bin/zsh",
    "EDITOR": "vi",
    "VISUAL": "vi",
    "CLAUDE_CONFIG_DIR": "/home/vscode/.claude"
  }
}
